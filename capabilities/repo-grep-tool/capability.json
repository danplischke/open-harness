{
  "id": "repo-grep",
  "name": "Repo Grep",
  "description": "Fast repository search backed by ripgrep. No MCP server required.",
  "kind": "tool",
  "tool": {
    "provider": "exec",
    "delivery": "cli",
    "exec": { "command": "rg", "args": ["--json"] },
    "usage": "rg --json '<pattern>' <path>"
  }
}
