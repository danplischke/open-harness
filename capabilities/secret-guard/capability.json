{
  "id": "secret-guard",
  "name": "Secret Guard",
  "description": "Denies any tool call whose input appears to contain a hardcoded secret (API key, private key, token). Blocking.",
  "run": { "command": "python3", "args": ["guard.py"] },
  "events": [
    { "phase": "pre", "subject": "tool", "tool_class": "any", "required": true }
  ]
}
